# PostgreSQL (matches docker-compose.yml by default)
DATABASE_URL=postgresql://ingestion:ingestion@localhost:5433/ingestion

# sentence-transformers model (384-dim; must match sql/002_schema.sql vector(384))
# ST_MODEL_NAME=all-MiniLM-L6-v2
