# Python Orchestrator – AI Translation Layer

Rules engine file for Claude Code. This project is the AI translation layer for the polyglot UI automation PoC: it produces LLM-generated JSON that the Java Selenium framework consumes from `test_data/generated_scripts/`.
