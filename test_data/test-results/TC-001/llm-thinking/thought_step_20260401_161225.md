# LLM Thinking: step

```
**Reasoning for Translating "Navigate to Dashboard"**

Okay, here's how I'm thinking through this translation. The task is to handle step 7: "Navigate to dashboard". The current application state is the "queue" page, and based on the macro plan, the next step *should* be `VerifyDashboardCount`.

My initial thought is to check if `VerifyDashboardCount` is directly applicable. Expanding that macro shows it *does* include the navigation steps I need: clicking the trading menu and then the dashboard link. However, it *also* includes verification (reading and asserting a trade count). The user's request is *only* for navigation.

The core of the problem here is whether to prioritize the macro plan (which says `VerifyDashboardCount` is next) even if it does more than the user's explicit request. I'm weighing the instruction to "align this step with the next unused macro" against the instruction to ensure intent is "fully covered".

I'm thinking about the "Available Actions" section and the guidance on when to use primitives: when no advanced action *matches* the intent, if the test explicitly targets low-level UI behavior, or when you only need a small assertion or navigation after a macro. The rule also says "prefer advanced actions...whenever the test intent is *fully covered* by one or more macros."

I'm re-evaluating the overall plan. The previous step was `ApproveTrade`, and the system is currently on the approval queue page. The macro plan clearly states `VerifyDashboardCount` is the next intended macro.

Here's the problem: I'd need to supply an `expectedCount` for `VerifyDashboardCount`. The natural language step does *not* provide an expected count, and the instruction is specifically "Translate step 7: 'Navigate to dashboard'". There's no instruction to verify the dashboard count at all. Thus, it seems like the instruction means I should *only* navigate.

I also considered the rule about not duplicating work done by an advanced action. But, the real issue here is *not* duplication. The issue is that the macro does too much! It tries to *add* functionality that's not requested.

Given the rule "Do NOT duplicate work already done by an advanced action's internal steps," it doesn't apply because there is no duplication. In fact, if I *were* to use the `VerifyDashboardCount` macro, it would mean duplicating information that I haven't been instructed to verify.

Therefore, because the user only asked for navigation, I think using primitives for just the navigation is the most appropriate. I looked at the expanded steps of `VerifyDashboardCount` to get those primitives: Clicking the menu trigger, then the dashboard link, and waits for visibility. These correspond to the "Navigation" section of the "Application Layout." The primitives are:
1.  `CLICK $trading-menu-trigger`
2.  `WAIT_VISIBLE $nav-dashboard`
3.  `CLICK $nav-dashboard`
4.  `WAIT_VISIBLE $dashboard-page`

I re-checked if "Navigate to dashboard" fully "matches" the `VerifyDashboardCount` macro per the instructions. It matches *part* of the macro, but the rule says "when the NL step *matches* it." It's not a full match. The intent is not *fully* covered by the macro if we take "fully covered" to mean "nothing more".

Using the primitives is more accurate in this scenario because it covers the exact intent of the NL step without unwanted side-effects, and aligns with the instructions.
```
