# LLM Thinking: step

```
**Translating "Logout" into JSON**

Okay, so I've got "Logout" as the natural language step to translate. My first instinct is always to check the "Planned macros" and the "Advanced Actions" sections – that's where I'll find my starting point. And there it is, right away, the "Planned macros" section has `Logout` listed as the next step. Excellent. Then I jump to the "Advanced Actions" section, just to be thorough, and confirm there's also a `Logout` macro defined there. Fantastic, it's consistent.

Now, let's look at the specifics. The action is clearly `Logout`, and it has no parameters, which makes things delightfully straightforward. Given that information, I'm going to map this to the JSON object `{ "action": "Logout", "locator": "", "test_input": "", "output": "" }`. It's a perfect match. I'll utilize the `Logout` macro as defined, the tcStep will be set to 5.
```
